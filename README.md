# pyChess
This is pyChess, a chess game made using python, which is very simple, pure text, so it is compilable with just the random library and the copy library.
It hase 2 modes:
Player vs Player
Player vs Bot

# HAVE FUN
